const plantRecommendation = require("./plant-rec");
const scanHistory = require("./scan-history");
const historyDetail = require("./history-detail")

module.exports = { plantRecommendation, scanHistory, historyDetail };
